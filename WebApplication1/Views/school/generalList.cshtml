@model IEnumerable<WebApplication1.Models.general>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<a href="@Url.Action("generalAdd","school")" class="btn px-15 btn-primary">
    Yeni Yönetici Ekle
</a>
<table class="table mb-0 table-borderless team-table" data-sorting="true"
       data-filter-container="#filter-form-container" data-paging-current="1"
       data-paging-position="right" data-paging-size="10">
    <thead>
        <tr class="userDatatable-header">
            <th>
                Id
            </th>
            <th>
                Adı Soyadı
            </th>
            <th>
                Görevi
            </th>
            <th>
                Tipi
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var generl in Model)
            {
                <tr>
                    <td>
                        <div class="userDatatable-content" style="align-items:center">
                            @generl.generalId
                        </div>
                    </td>
                    <td>
                        <div class="userDatatable-content">
                            @generl.name
                        </div>
                    </td>
                    <td>
                        <div class="userDatatable-content">
                            @generl.objective
                        </div>
                    </td>
                    <td>
                        <div class="userDatatable-content">
                            @generl.type
                        </div>
                    </td>
                    <td>
                        <ul class="orderDatatable_actions mb-0 d-flex flex-wrap">
                            <li>
                                <a href="@Url.Action("generalEdit","school", new {  id=generl.generalId  })">
                                    Güncelle
                                </a>
                            </li>
                            <li>
                                <a href="@Url.Action("generalRemove","school", new {  id=generl.generalId  })">
                                    Sil
                                </a>
                            </li>
                        </ul>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3">&nbsp;</td>
            </tr>
        }
    </tbody>
</table>