@model IEnumerable<WebApplication1.Models.tolesson>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<a href="@Url.Action("toLessonAdd","school")" class="btn px-15 btn-primary">
    Yeni Ders Atama
</a>
<table class="table mb-0 table-borderless team-table" data-sorting="true"
       data-filter-container="#filter-form-container" data-paging-current="1"
       data-paging-position="right" data-paging-size="10">
    <thead>
        <tr class="userDatatable-header">
            <th>
                Id
            </th>
            <th>
                Dersi Alan
            </th>
            <th>
                Ders
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var toLEsson in Model)
            {
                <tr>
                    <td>
                        <div class="userDatatable-content" style="align-items:center">
                           @toLEsson.Id
                        </div>
                    </td>
                    <td>
                        <div class="userDatatable-content">
                            @toLEsson.student.name
                        </div>
                    </td>
                    <td>
                        <div class="userDatatable-content">
                            @toLEsson.lesson.name
                        </div>
                    </td>
                    <td>
                        <ul class="orderDatatable_actions mb-0 d-flex flex-wrap">
                            <li>
                                <a href="@Url.Action("toLessonEdit","school", new {  id=toLEsson.Id  })">
                                    Güncelle
                                </a>
                            </li>
                            <li>
                                <a href="@Url.Action("toLessonRemove","school", new {  id=toLEsson.Id  })">
                                    Sil
                                </a>
                            </li>
                        </ul>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3">&nbsp;</td>
            </tr>
        }
    </tbody>
</table>